<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MasterFloorAPP.PartnerSalesPage"
             Title="История продаж">
    <Grid>
        <VerticalStackLayout Spacing="10" Padding="10">
            <Label Text="{Binding PartnerName}" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Список продаж:" FontAttributes="Bold" FontSize="16" />

            <ListView ItemsSource="{Binding Sales}" HasUnevenRows="True" HeightRequest="500">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Padding="5" ColumnDefinitions="*, Auto, Auto">
                                <Label Grid.Column="0" Text="{Binding ProductName}" FontAttributes="Bold" />
                                <Label Grid.Column="1" Text="{Binding Quantity}" HorizontalOptions="End" Margin="10,0" />
                                <Label Grid.Column="2" Text="{Binding SaleDate, StringFormat='{0:dd.MM.yyyy}'}" />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>